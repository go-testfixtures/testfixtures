posts{1..10}:
  - id: <current>
    title: Post <current>
    content: "{{fakeParagraph 3}}"
    created_at: "{{fakeTimestamp}}"
    updated_at: "{{fakeTimestamp}}"

posts_tags{1..8}:
  - post_id: <current>
    tag_id: "{{fakeIntBetween 1 3}}"

comments{1..30}:
  - id: <current>
    post_id: "{{fakeIntBetween 1 10}}"
    content: Post comment <current>
    author_name: "{{fakePersonName}}"
    author_email: "{{fakeEmail}}"
    created_at: "{{fakeTimestamp}}"
    updated_at: "{{fakeTimestamp}}"

